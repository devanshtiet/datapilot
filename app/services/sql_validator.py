"""
app/services/sql_validator.py
==============================
SQL query validation (SELECT-only, whitelist, row-limit enforcement).
STUB — implemented in Mission 3.
"""

def validate_sql(query: str) -> bool:
    """
    SQL validator stub for Mission 1 & 2.
    Full SELECT-only validation added in Mission 3.
    """
    return True

